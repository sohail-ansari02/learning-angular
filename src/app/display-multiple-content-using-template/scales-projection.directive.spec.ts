import { ScalesProjectionDirective } from './scales-projection.directive';

describe('ScalesProjectionDirective', () => {
  it('should create an instance', () => {
    const directive = new ScalesProjectionDirective();
    expect(directive).toBeTruthy();
  });
});
